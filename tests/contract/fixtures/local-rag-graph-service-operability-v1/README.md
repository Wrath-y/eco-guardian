# local-rag graph service operability v1 fixtures

This directory is an unchanged vendor copy of
`local-rag/tests/contract/fixtures/graph-service-operability-v1` fixture
version `1.0`. It is a consumer-test input only: Eco Guardian must not modify
its provider semantics or regenerate these payloads.

The corresponding provider OpenAPI document is `local-rag/api/openapi.yaml`
with SHA-256:

```text
fd39c71846e49f0f6a7b4b1dc69a089634006af002d36af58c61444611df9369
```

The upstream `manifest.json` remains authoritative for every JSON fixture in
this directory. Updating this copy requires reviewing the upstream contract
version, document digest, and fixture digests together.
