// Package materialization turns exact, immutable revision inputs into typed
// rule graphs. It is deliberately independent of SQLite, HTTP, Graph, AI,
// working state, and mutable simulation state.
package materialization
