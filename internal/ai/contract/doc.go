// Package contract owns transport-neutral, immutable AI workflow contracts.
//
// It must not depend on persistence, HTTP, provider, retrieval, revision-write,
// release, or graph-activation implementations.
package contract
