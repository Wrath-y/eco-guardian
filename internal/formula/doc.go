// Package formula contains the deterministic, versioned DSL mechanics.
//
// It intentionally has no dependency on HTTP, storage, UI, providers, or AI.
// Adapters supply symbols and persist any derived data outside this package.
package formula
